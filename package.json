{
  "name": "pact-js-bug-cannot-merge-v3-pacts",
  "version": "1.0.0",
  "description": " Minimal example of a bug in pact-js: spec `v3` Pact files cannot be merged when publishing",
  "scripts": {
    "test": "docker compose up -d && rimraf pacts1 && rimraf pacts2 && mocha && node ./publish.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "@pact-foundation/pact": "^11.0.2",
    "axios": "^0.27.2",
    "chai": "^4.3.6",
    "mocha": "^9.2.2",
    "mocha-pact": "^0.10.1",
    "rimraf": "^3.0.2"
  }
}
